# Utils Package

`airfoil_dt.utils` is for small shared utilities that are genuinely reused across packages.

Avoid putting core dataset, model, training, evaluation, or digital-twin logic here. Those concerns have dedicated packages.
